services:
  web:
    build:
      context: .
      dockerfile: apps/www/Dockerfile
      args:
        VITE_SENTRY_DSN: ${VITE_SENTRY_DSN}
        VITE_SENTRY_ENABLED: ${VITE_SENTRY_ENABLED}
    ports:
      - "3000:3000"
    volumes:
      # Mount local data directory for SQLite persistence
      - ./apps/www/data:/app/data
    env_file:
      - path: ./apps/www/.env.development.local
        required: false
      - path: ./apps/www/.env.development
        required: false
      - path: ./apps/www/.env
        required: false
      - path: .env
        required: false
    environment:
      NODE_ENV: development
      DATABASE_URL: file:/app/data/development.db
    healthcheck:
      test:
        [
          "CMD",
          "node",
          "-e",
          "require('http').get('http://localhost:3000/api/health', (r) => {process.exit(r.statusCode === 200 ? 0 : 1)})",
        ]
      interval: 30s
      timeout: 3s
      start_period: 5s
      retries: 3
    restart: unless-stopped
