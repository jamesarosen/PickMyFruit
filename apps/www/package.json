{
  "name": "@pickmyfruit/www",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "astro": "astro",
    "build": "astro build",
    "deploy": "fly deploy",
    "dev": "astro dev",
    "docker:build": "docker build -f ./Dockerfile --target runtime --tag pickmyfruit/www:latest ../..",
    "preview": "astro preview"
  },
  "files": [
    "dist"
  ],
  "dependencies": {
    "@astrojs/node": "^9.0.0",
    "@pickmyfruit/env": "workspace:*",
    "astro": "^5.1.1"
  },
  "devDependencies": {
    "@astrojs/ts-plugin": "^1.10.4"
  }
}
