The `src/styles/` directory is for global styles.

Component-specific styles go in `src/components/` next to the relevant component.

Page-specific styles go in `src/pages/` next to the relevant page.
