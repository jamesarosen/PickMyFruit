/**
 * Entry point for the resend-sync worker.
 *
 * Subsequent commits fill in env parsing, the cursor file, the token bucket,
 * the Resend client, processOneRow, and runCycle. This stub exists so the
 * tsc build and Docker stage have something to compile from commit one.
 */

process.stdout.write("resend-sync: worker stub — implementation pending\n");
